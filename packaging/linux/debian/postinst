#!/bin/sh
set -eu

install -d -m 0750 /var/log/ipad-manager
if command -v update-desktop-database >/dev/null 2>&1; then
    update-desktop-database /usr/share/applications >/dev/null 2>&1 || true
fi
/usr/bin/ipad-manager-health --json > /var/log/ipad-manager/install-health.json
